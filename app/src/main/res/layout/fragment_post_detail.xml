<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rv_post_details"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_marginTop="8dp"
            app:layout_constraintBottom_toTopOf="@id/comments_et_separator"
            app:layout_constraintTop_toTopOf="parent" />

        <View
            android:id="@+id/comments_et_separator"
            android:layout_width="match_parent"
            android:layout_height="12dp"
            android:background="@color/cloudy_blue"
            app:layout_constraintTop_toBottomOf="@id/rv_post_details"/>

        <com.likeminds.feedsx.branding.customview.edittext.LikeMindsEditText
            android:id="@+id/et_comment"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:background="@null"
            android:ellipsize="end"
            app:font_style="regular"
            android:paddingHorizontal="16dp"
            android:inputType="textMultiLine"
            android:minHeight="48dp"
            android:hint="@string/write_a_comment"
            android:textColor="@color/dark_grey"
            android:textColorHint="@color/maastricht_blue_40"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toStartOf="@id/iv_comment_send"
            app:layout_constraintTop_toBottomOf="@id/comments_et_separator"
            app:layout_constraintBottom_toBottomOf="parent" />

        <ImageView
            android:id="@+id/iv_comment_send"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toTopOf="@id/et_comment"
            app:layout_constraintBottom_toBottomOf="@id/et_comment"
            app:layout_constraintEnd_toEndOf="parent"
            app:srcCompat="@drawable/ic_comment_send_disable"
            tools:ignore="ContentDescription"/>

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>